/**
 * React entrypoint (optional peer dependency).
 *
 * Placeholder for M1. Wagmi-based hooks (useCampaign, useIdentityGate, useClaim)
 * land alongside the frontend stream once contract ABIs are finalized (W7).
 * Keeping the subpath export stable now so consumers can import from
 * `@tokamak-network/scatter-drop-sdk/react` without a breaking change later.
 */
export {};
