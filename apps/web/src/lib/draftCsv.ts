/**
 * localStorage key that hands a CSV recipient list from the /tools builder to
 * the create wizard ("Use in a campaign"). Shared so both sides stay in sync.
 */
export const DRAFT_CSV_KEY = "scatterdrop:draft-csv";
